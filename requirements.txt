# Dependencias del bot — versiones fijadas con compatible-release (~=) para
# permitir parches de seguridad automáticos sin romper la API.
discord.py~=2.7
aiosqlite~=0.22
python-dotenv~=1.0
python-a2s~=1.4
tzdata>=2026.2

# Testing
pytest~=9.0
pytest-asyncio~=1.3
pytest-mock~=3.15
